# 17 · 模块化任务书（可直接分配）

> ## ⚠️ 2026-09-13 重大修正：产品形态已改，本任务书已同步重写
>
> **形态改为：悬浮窗只「读屏」+ 主站承载所有功能。见 [`docs/20-产品形态定义.md`](20-产品形态定义.md)。**
>
> | 变化 | 影响本任务书 |
> |---|---|
> | 悬浮窗**不再控制宿主页面**（不点按钮、不驱动动画、不叠加元素） | 原 `X2` 的"划词闭环"**作废**；新增 `X3` 只读改造 |
> | 功能重心移到**主站**（对标 408os：题库 / 刷题二步提示 / 错题本 / 知识图谱） | **新增 `X4`–`X8` 五个主站任务** |
> | 批改与题库从"用不到"变成"主线核心" | `C2` `K3` `K4` `X1` `X2` **升级为 P0** |
> | 本轮**不做**热力图与 PDF 溯源 | 不进入任何任务 |
>
> 逐条处置对照表见 `docs/20` §三。**旧任务编号含义已变，接手前先读那一节。**

> **本文档的唯一用途**：把当前**还没做完**的工作切成可独立交付、可独立验收的模块。
>
> | 项 | 说明 |
> |---|---|
> | 读者 | 要动手写代码的人、要评审 PR 的人 |
> | 不负责 | 产品形态（→ `docs/20`）、阶段计划（→ `docs/12`）、协作规范（→ `docs/04`）、设计理由（→ `docs/10`、`docs/adr/`） |
> | 快照 | 分支 `dev`，2026-09-13 |
> | 依据 | `docs/20`（形态）· `docs/12`（M0–M6 阶段）· `docs/09` §二（A/B/C/D 验证分级）· `contracts/`（契约即接口） |
>
> ⚠️ **本文档里每个"实测"都是有人在快照 commit 上跑出来的**，跑法写在 §七。
> 如果你读到某个数字与现状不符，**先怀疑这份文档过期了，按 §七 重新测一遍再改它**。

---

## 一、任务契约（每个任务必须写全这 8 项）

之前那版任务书最大的问题是**任务与理由、记录、数字混在一段散文里**，读的人分不清
"这是要我做什么"还是"这是前人踩过的坑"。本版把任务压成一张固定结构的卡片：

| 字段 | 含义 | 不写会怎样 |
|---|---|---|
| **交付物** | 做完之后，什么东西从"没有"变成"有" | 任务变成一句口号，谁都不知道何时算完 |
| **现状证据** | `文件:行` + 一句实测 | 别人无法判断这个问题还存不存在 |
| **写入范围** | 这个任务**独占可写**的文件 | 两个人同时改一个文件 → 返工 |
| **禁止触碰** | 明确不能改的文件 | 顺手改了共享文件 → 打崩别人 |
| **接口冻结** | 本任务不能改的契约 | 各写各的字段 → 联调才发现对不上 |
| **验收** | **一条能跑的命令** + 期望输出 | "人工看一眼"等于没验收 |
| **依赖** | 必须先完成的任务 | 按不存在的接口写代码 |
| **规模** | 粗估（S ≤ 半天 / M ≤ 2 天 / L > 2 天） | 排期失真 |

**合并规则**：一个任务 = 一个 PR = 一个 squash 提交。分支 `feat/<任务ID>-<简述>`。

---

## 二、状态总览

图例：✅ 已完成并实测 · 🔶 部分完成（有残留） · ⬜ 未开工 · 🚫 阻塞（缺外部资源）
优先级：**P0** = 主线核心，本轮必须做 ｜ P1 = 该做 ｜ P2 = 可延后

| 工作流 | 任务 | 状态 | 优先级 | 规模 | 依赖 |
|---|---|---|---|---|---|
| **G · 门禁与基线** | G1 CI 与本地基线修复 | ✅ | — | M | — |
| | G2 文档漂移收敛（**含本次形态修正的回收**） | 🔶 | P1 | M | — |
| **C · 正确性保障链** | C1 验证报告契约收敛 | ✅ | — | L | — |
| | **C2 A/B 级真的可达（数值重算 + 符号校验）** | ⬜ | **P0** | **L** | C1 |
| | C3 HITL 触发口径收敛（按风险，不按题型） | 🔶 | P2 | M | C1 |
| | C4 多路独立求解 + 分歧不投票 | ⬜ | P2 | L | C2 |
| **K · 内核与能力接入** | **K1 工具门控真正生效** | ⬜ | **P0** | S | — |
| | K2 工具结果 ok/empty 真实化 | ⬜ | P1 | S | K1 |
| | **K3 批改闭环接入 Agent 链路** | ⬜ | **P0** | M | C1 |
| | **K4 学习记录工具接入（错题本的数据源）** | ⬜ | **P0** | S | — |
| | K5 诊断类工具接入（孤儿工具归零） | ⬜ | P1 | S | — |
| **X · 悬浮窗（只读）** | **X3 悬浮窗只读改造 + 主站入口** | ⬜ | **P0** | M | X4 |
| | **X2 抽取加固（读屏能力）+ SSE 人话提示** | ⬜ | **P0** | M | — |
| | X1 前端展示验证级别 / 批改依据 | ⬜ | **P0** | M | C1 |
| **H · 主站（功能重心）** | **X4 主站入口壳（本轮唯一明确要做的壳）** | ⬜ | **P0** | M | — |
| | X5 题库 + 刷题（二步提示） | ⬜ | P1 | L | K3 |
| | X6 错题本 | ⬜ | P1 | M | K4 |
| | X7 知识图谱（需从零建 DAG） | ⬜ | P1 | L | D3 |
| | X8 动画库 / 分布可视化归位 | ⬜ | P2 | S | X4 |
| **E · 评估** | E1 题库扩到 ≥100 题（充实现有 core.json） | ⬜ | P1 | L | — |
| **D · 需决策/外部** | D1 M0 剪枝口径裁定 | ✅（裁定：不剪枝） | — | S | — |
| | D2 真实平台读屏验证（M5） | 🚫 | **P0（等资源）** | M | 平台账号 |
| | D3 教材语料入库（L1） | 🚫 | P2 | M | 老师提供教材 |

### 关键路径

```
G1 ✅ ─ C1 ✅ ─┬─ C2 (P0)
              ├─ K3 (P0) ─→ X5 题库+刷题
              └─ X1 (P0)

K1 (P0) ── K2
K4 (P0) ── X6 错题本
X2 (P0) ── X3 只读改造 ──（X4 入口壳）
X4 (P0) ──┬─ X8 动画库/可视化归位
          └─ X5 · X6 · X7

X7 知识图谱 ← 需要 D3 教材 / 或先用自建知识点列表起步
E1 题库扩充（与 X5 强相关，建议同一人）
D2 真实平台读屏验证 ← 需平台账号（本轮最该去要的资源）
```

**当前可同时开工的独立模块（彼此无写入冲突）**：
`C2` · `K1` · `K3` · `K4` · `X2` · `X4` · `E1` —— **7 个 P0/P1 可立即开工**。
`K2` 排在 `K1` 后（同文件）；`X3` 排在 `X4` 后（要有个地方可跳）；`X5`/`X6` 分别排在 `K3`/`K4` 后。

### 🆕 主站五件（本轮的真正工作量所在）

如果你今天要给人派活，**重头在这里**——现有代码几乎没有覆盖：

| 任务 | 一句话 | 现有底子 |
|---|---|---|
| `X4` 主站入口壳 | 悬浮窗能进来的那个门 | `frontend/app/` 9 个展示页可改造 |
| `X5` 题库 + 刷题 | 做题、判定、**二步提示**、看解析 | 题库 JSON 结构已就绪；`grade_answer` 引擎可用 |
| `X6` 错题本 | 错题归类、重做 | `qa_log` 已有 115 行数据；`mastery` 表待接入 |
| `X7` 知识图谱 | 知识点网络 + 依赖 + 进度着色 | **从零建**（现无 DAG） |
| `X8` 动画库 / 可视化归位 | 把现有展示页改成主站功能页 | `features/animations`、`features/visualize` 已有 |

> ⚠️ **`X7` 与旧文档冲突**：`docs/14` §七 把"知识点 DAG 级联定位"列为**明确不做**，
> `docs/10` §三 列为待拍板。**本次形态修正推翻了那个决定** —— 知识图谱现在是主站要求的功能。
> `docs/20` §三 已记录，G2 负责回收旧表述。

### 接续说明（相对上一版的变化）

**变化一 · 已完成项被复核**（上一版把 A1/E1 当待办、把 B1 当未开工）：

| 上一版说法 | 实测结论 | 依据 |
|---|---|---|
| `verification.report` 从未出现 → 任务 `A1` | ✅ **已发出**，`verifier.py:292`，事件流实测命中 | 见 C1 卡片 |
| 真实事件流没有 `hitl.request` → 任务 `B1` | 🔶 **已发出**，但**触发口径错误**（按题型而非按风险） | 见 C3 卡片 |
| `E1` 待做 | ✅ 已完成（`a0923d8`），CI 已全量 | `ci.yml` |
| — | ⬜ 新增：**前端对 `verification.report` 零处理** | 见 X1 卡片 |

**变化二 · 产品形态修正（2026-09-13，见 `docs/20`）**：

| 变化 | 影响 |
|---|---|
| 悬浮窗**只读**，不再控制宿主页面 | 原 `X2` 的"划词闭环"**作废**；新增 **`X3` 只读改造** |
| 功能重心移到**主站** | 新增 **`X4` 入口壳 · `X5` 题库刷题 · `X6` 错题本 · `X7` 知识图谱 · `X8` 动画库归位** |
| 批改 / 题库成为主线核心 | `C2` `K3` `K4` `X1` `X2` **升级为 P0** |
| 本轮不做热力图与 PDF 溯源 | 不进入任何任务 |
| 知识图谱成为要求 | 推翻 `docs/14` §七"知识点 DAG 明确不做"的决定 |

> 📌 上一版的 **A1 / E1 详细记录**（含踩坑：模块级 `async def run` 遮蔽局部变量）
> 已移入 `docs/archive/17-模块化任务书-v1.md`，需要考古时再翻。

---

## 三、任务卡片

### 工作流 G · 门禁与基线

#### G1 · CI 与本地基线修复 ✅ 已完成
- **交付物**：CI 全量跑单测；契约检查不再"假绿"；12 个脚本在中日文 Windows 下不再崩溃
- **落地位置**：`.github/workflows/ci.yml`、`scripts/_console.py`、`scripts/check_contracts.py`、`backend/tests/test_contract_events.py`
- **验收（可复跑）**：`python -m pytest backend/tests -q` → 全量收集；`python scripts/check_contracts.py` → 全绿（当时 40 项，现 46 项）
- **提交**：`a0923d8`

#### G2 · 文档漂移收敛 🔶
- **交付物**：**每一类事实只有一处权威**；`README.md` / `docs/12` / `docs/14` / `docs/15` 不再自相矛盾
- **现状证据**（实测，快照 `3ce581c`）

  | 位置 | 写的 | 实测 |
  |---|---|---|
  | `README.md:130,212`、`docs/14:264`、`docs/15:45` | 15 个工具 | **16** |
  | `README.md:114`、`README.md:130` 附近 | 13 种事件 | **14** |
  | `README.md:179` | `test_page_context` 15 项 | 实测该文件独立项数见 §七 |
  | `README.md:17` vs `README.md:48` | 执行依据 `docs/09` vs 已被 `docs/12` 取代 | **同一文件自相矛盾** |
  | `docs/14:97` | 单测 70 项 | **100 收集** |
  | `docs/14:263` | "PR #7 待合并" | 早已合并；快照上**最高 PR 号为 #15** |
  | `docs/12:94`、`docs/11:161` | tag `v0.1.0-prototype` | 只有 **`v0.1.0`** |
  | `docs/15:78` vs `docs/15:111-132` | "批改闭环整体缺失" vs "最小版已落地" | **同文件自相矛盾** |
  | `contracts/README.md:28` | 权威实现 `kernel/verification.py` | **该文件不存在**，实际是 `agents/verifier.py` |
  | `README.md` 自检清单 | 缺 `check_contracts.py`、缺全量 `pytest` | 新人必踩 |

- **写入范围**：`README.md`、`docs/11`、`docs/12`、`docs/14`、`docs/15`、`docs/16`
- **禁止触碰**：`docs/17`（本任务书）、`docs/18`（文档地图）—— 数字口径以它们为准
- **验收**：`python scripts/check_docs.py`（**本任务需新增**）逐条断言上表；上表清零
- **注意**：**不要手工改 §七 的数字**，那是唯一基线口径；`docs/16` 由 `eval_retrieval.py` 重生成
- **依赖**：无 · **规模** M

---

### 工作流 C · 正确性保障链（本项目的差异化）

#### C1 · 验证报告契约收敛（`verification.report`）✅ 已完成
- **交付物**：`verification.report` 成为验证结论的**唯一权威出口**，且通过 `verification.schema.json` 校验
- **落地位置**：`backend/app/agents/verifier.py:292`（事件出口）、`backend/tests/test_verifier.py`（6 项）
- **实测验收**（快照 `3ce581c`，mock provider）

  | 提问 | 事件流尾部 | `verification.report` |
  |---|---|---|
  | 数值题 | `… tool.result → verification.report → artifact → agent.end → hitl.request` | ✅ |
  | 概念题 | `… tool.result → verification.report → artifact → agent.end → run.end` | ✅ |

- **口径（沿用 `docs/09` §二，未新造）**

  | 级别 | 题型 | 允许的 `method` | `confidence` 上限 |
  |---|---|---|---|
  | A | 纯数值题 | `sympy_recompute` + `multi_path` | 1.0 |
  | B | 公式 / 恒等变形 | `symbolic_equivalence` | 0.95 |
  | C | 应用题 | `multi_path` + `constraint` + `back_substitute` | 0.85 |
  | D | 证明 / 开放题 | `heuristic`（`passed=false`） | 0.4 |

- **两道封顶**（落地时确立，后来者照此理解）
  1. **题型封顶**：证明题无论检索到多少依据，只报 D
  2. **手段封顶**：木桶原理 —— 只要有一条检查是 `heuristic`，整体退到 D，**不取最强的那条**
- **提交**：`3ce581c`（PR #15）

> ⚠️ **C1 完成 ≠ 主线承诺兑现**：因为 `sympy_recompute` / `symbolic_equivalence`
> 这两类检查**目前根本没有实现在 verifier 里**，所以 A/B 级**恒不可达**，
> 所有题目实际落在 C 或 D。这正是 **C2** 要解决的。

#### C2 · A/B 级真的可达（数值重算 + 符号等价性）⬜ ★ 最高优先级
- **交付物**：`verifier` 的 `checks` 里**首次出现** `method=sympy_recompute` 与 `symbolic_equivalence`，
  从而使 `_achieved_level()` 能推出 A / B（级别是**推导出来的**，不是写死的）
- **现状证据**
  - `backend/app/kernel/specs.py` 之外，`backend/app/domain/formalize.py` **不存在** ——
    `contracts/README.md:29` 把 `math-task.schema.json` 的权威实现指向了它（这是 G2 要修的一处漂移）
  - 因此 `MathTask`（`contracts/math-task.schema.json`）**只有 schema，没有产出方**
  - 实测：数值题最终级别为 **C**，证明题为 **D**（见 C1 表格）
- **要做的三步**
  1. `backend/app/domain/formalize.py`：NL 题干 → `MathTask{known, goal, constraints, domain}`，
     每个已知量必须能追溯到 `source_text` 原句（**没有出处的已知量就是幻觉来源**）
  2. `backend/app/tools/verify_numeric.py`：对 `MathTask` 的数值目标做 SymPy 高精度独立重算，
     产出一条 `method=sympy_recompute` 的 check（不一致 → `passed=false` + `evidence.expected/actual`）
  3. `verifier.py` 消费上述结果：数值题接 `sympy_recompute`，符号题接 `symbolic_equivalence`
- **写入范围**：`backend/app/domain/formalize.py`（新增）、`backend/app/tools/verify_numeric.py`（新增）、
  `backend/app/agents/verifier.py`
- **禁止触碰**：`contracts/**`（`math-task.schema.json` 已是冻结件，**实现向它对齐，不是反过来**）、
  `backend/app/kernel/specs.py`（K1/K2 独占）
- **接口冻结**：`contracts/math-task.schema.json`、`contracts/verification.schema.json`
- **验收（可机验）**
  1. `python -m pytest backend/tests/test_verify_numeric.py -q`（新增）→ 全绿
  2. `python -m pytest backend/tests/test_verifier.py -q` → 新增断言：**数值题报告的 `level == "A"`**，
     且 `checks` 中存在 `method == "sympy_recompute"`；**符号题 `level == "B"`**
  3. **负向测试**：注入一个算错的答案，重算必须拦下 → `passed=false`，`evidence.expected != evidence.actual`
  4. 形式化准确率：50 道题抽样 ≥ 85%（`docs/09` M1.1 的口径），脚本与题集随任务提交
- **依赖**：C1（✅） · **规模 L** · **风险**：形式化错会把整条链带偏 →
  必须**只对形式化成功的题**升 A/B，形式化失败一律退回 C 并如实写进 `unsupported`

#### C3 · HITL 触发口径收敛（按风险，不按题型）🔶
- **交付物**：`hitl.request` 只在**高风险结论**（解题类）出现，不再对概念/证明题提问打断
- **现状证据**（实测，mock provider）

  | 提问 | 意图 | `hitl.request` | 应该 |
  |---|---|---|---|
  | 数值求解题 | solve | ✅ 出现 | ✅ |
  | 概念解释"贝叶斯公式" | knowledge | ❌ 不出现 | ❌ |
  | **证明题** | solve | ✅ 出现 | ⚠️ 存疑：`docs/12:151` 只要求"高风险结论（解题）"确认 |
  | — | — | — | **`docs/02:62` 只说"图挂起时发"，未定义"什么该挂起"** |

  代码依据：`backend/app/agents/verifier.py:238-247` 在两处置 `needs_human = True`
  （证明类、无依据类）→ `kernel/graph.py:214` 据此挂起 → `kernel/runner.py:96` 补发事件。
  **链路是通的**，缺的是**口径文档与阈值**：判定条件写在 verifier 代码里，契约里没有定义。
- **要做的**
  1. 在 `contracts/README.md`（或 `contracts/INDEX.md`）**写死触发口径**：
     哪种 `intent` × 哪种 `needs_human` 原因 → 必须/禁止挂起（当前实现是"任一原因即挂起"）
  2. 按口径调整 `verifier.py` 的 `needs_human` 判定，使"证明题不打断、解题题必打断"
  3. `hitl.request` 的 `draft` 目前取自 `interrupt_payload["draft"]`（`graph.py:221`）；
     确认该字段在真实链路里非空（实测为空串时前端 HITL 卡片是空白的）
- **写入范围**：`backend/app/agents/verifier.py`、`backend/tests/test_kernel.py`（HITL 用例）、
  `contracts/README.md`（仅 HITL 口径一节）
- **禁止触碰**：`backend/app/kernel/graph.py`、`kernel/runner.py`（链路已通，**除非实测证明不通**）
- **验收（可机验）**
  1. 解题类提问事件流**必须**含 `hitl.request`；概念类**必须不含**
  2. `/api/hitl/{run_id}/resolve` 三种 action（`confirm` / `correct` / `supplement`）各走通一次，
     且 `correct` 的文本**回流到 `run.end.final_answer`**
  3. 新增参数化测试覆盖上述矩阵
- **依赖**：C1（✅） · **规模 M**

#### C4 · 多路独立求解 + 分歧不投票 ⬜
- **交付物**：同一题由 **≥2 条策略不同**的路径独立求解；结论不一致时产出
  `verification.report.disagreements`（非空），**不做多数投票**
- **现状证据**：`contracts/verification.schema.json:31-52` 定义了 `disagreements`，
  但全仓库**没有任何代码产出它**（该字段只在 schema 的 examples 里出现过）
- **关键设计**（`docs/09:111`）：**多路 ≠ 同一次调用的重试**。
  三个样本会共享同一个错误前提；必须换策略：`direct` / `case_split` / `specialize` / `reverse`
- **写入范围**：`backend/app/agents/`（新增策略节点，一人一文件）、`backend/app/kernel/graph.py`（fan-out）、
  `backend/tests/test_multi_path.py`（新增）
- **禁止触碰**：`contracts/**`
- **验收（可机验）**
  1. 新增测试：构造一个两条路径必然分歧的题 → `disagreements` 非空、`paths` 含两条不同标识
  2. 分歧存在时 **`level` 不得为 A**，且必须触发 C3 定义的 HITL
  3. 正常题不产生噪音：`disagreements` 缺省不出现
- **依赖**：C2（路径要能独立算出数值才能比对） · **规模 L**

---

### 工作流 K · 内核与能力接入

#### K1 · 工具门控真正生效 ⬜ ★ 安全缺口
- **交付物**：Agent 只能调 `AgentSpec.tools` 里声明的工具，越权调用**抛错且可见**
- **现状证据**
  - `contracts/agent.schema.json` 承诺"调用未授权工具会被 ToolRegistry 拒绝"
  - `backend/app/kernel/specs.py:99-101` 门控逻辑**写好了**
  - 但唯一入口 `backend/app/kernel/specs.py:200` 写死 `allowed=None` → **门控永不触发**
  - **实测（不是推断）**：拿 `orchestrator`（`SPEC.tools = ()`）调 `ctx.use("kb_search", …)`
    → **成功返回 4 个片段**，无异常。即：能力边界当前是**装饰性的**
- **写入范围**：`backend/app/kernel/specs.py`、`backend/tests/test_kernel.py`（追加用例）
- **禁止触碰**：`backend/app/agents/_router.py`（路由表，K5 可能要用）
- **验收（可机验）**
  1. 用例：把当前 Agent 换成 `SPEC.tools` 不含 `kb_search` 的 Agent，`ctx.use("kb_search")` → **必须抛 `PermissionError`**
  2. 用例：`verifier` 调 `kb_search` **不得**回归报错（它确实声明了）
  3. `python -m pytest backend/tests -q` 全绿 —— 7 个 Agent 现有调用**全部不回归**
- **依赖**：无，**可立即开工** · **规模 S** · ⚠️ **注意**：这是行为收紧，
  有可能**立刻暴露出若干个 Agent 在偷偷调未声明的工具** —— 那是**真实缺陷**，修 Agent 而不是放宽门控

#### K2 · 工具结果 `ok` / `empty` 真实化 ⬜
- **交付物**：`tool.result.ok` 反映真实执行结果；"查不到"与"失败"在前端可区分
- **现状证据**
  - `contracts/tool.schema.json:56-67` 要求三态：`ok`（成功）/ `empty`（成功但无结果）/ `available`（能力是否具备）
  - `backend/app/kernel/specs.py:190` 把事件里的 `ok` **硬编码为 `True`**，且 `specs.py:104` 调工具时**不 try/except**
  - 实测 `tool.result` 报文：`{"type":"tool.result","ok":true,…}`，而 `kb_search` 在无命中时
    自报 `empty:true` 却**只出现在 `data` 里**，事件层 `ok` 仍是 `true`
  - 后果：`frontend/app/components/trace.js:194` 的失败分支**永不触发**；工具抛异常时链路是否有事件也未经测试
- **写入范围**：`backend/app/kernel/specs.py`、`backend/app/tools/*.py`、`backend/tests/test_kernel.py`
- **禁止触碰**：`frontend/app/components/trace.js`（X1 独占）
- **验收（可机验）**
  1. 构造必然失败的工具调用（不存在的 tool id、参数非法、工具内抛异常）→ 事件 `ok == false`，
     且**有 `error` 文案**、SSE 流**不断**
  2. 检索无命中 → `ok == true` **且** `empty == true`（不能报成失败，否则 Agent 会反复重试）
  3. 无页面上下文时 `page_search` → `available == false`
  4. 断言"前端能拿到非 true 的 ok"
- **依赖**：**K1**（同文件 `specs.py`，串行） · **规模 S**

#### K3 · 批改闭环接入 Agent 链路 ⬜
- **交付物**：用户说"批改这道题"能**真的走到**批改路径，并回传判定 + 首个不一致步
- **现状证据**
  - `backend/app/tools/grader.py:508` 有 `@tool("grade_answer")`，521 行实现 + 10 道已核验题库 + **16 项测试**齐全
  - 但 **7 个 AgentSpec 无一声明它**（`analytics` / `knowledge` / `orchestrator` / `page_tutor` /
    `solver` / `verifier` / `visualizer` 的 `tools=` 全部实测列出，见 §七）
  - `backend/tests/test_grader.py` 只有 `test_grader_is_discovered_as_tool` 与
    `test_grade_answer_tool_wraps_grade` **直接调用** → 用户在链路里**用不到**
- **写入范围**：`backend/app/agents/grader.py`（新增 Agent）**或** `backend/app/agents/solver.py`（并入）、
  `backend/app/agents/_router.py`（加意图关键词）、`backend/tests/test_grader.py`
- **禁止触碰**：`backend/app/tools/grader.py` 的既有算法（已核验，改动需 R3 复核）
- **验收（可机验）**
  1. 真实提问"批改这道题：<题干>；我的答案是 …"→ 事件流出现 `tool.call(tool=grade_answer)`
  2. 回传含**判定**与**首个不一致步**
  3. `grade_answer` **不再出现在孤儿工具列表**（K5 的断言会覆盖）
  4. 批改结论同样产出 `verification.report`（与 C1 同一出口）
- **依赖**：C1（✅） · **规模 M** · ⚠️ 若新增 Agent，需要同步 `_router.py` —— **该文件是共享可变文件**，见 §六

#### K4 · 学习记录工具接入 ⬜
- **交付物**：一次问答后学习记录**真的落库**，薄弱排序反映新记录
- **现状证据**：`log_qa` / `log_mastery`（`backend/app/tools/learning_log.py`）**无 AgentSpec 声明、无人调用**。
  危险在于"**以为在记录，其实没记**" —— 这是静默失败，最难发现
- **写入范围**：`backend/app/agents/analytics.py`、`backend/tests/test_kernel.py`（追加）
- **验收（可机验）**
  1. 一次问答后 `learning.sqlite` 的 `qa_log` **新增行数 ≥ 1**
  2. `/api/stats/{session_id}` 的薄弱排序反映新记录
- **依赖**：无 · **规模 S**

#### K5 · 诊断/枚举类工具接入（孤儿工具归零）⬜
- **交付物**：**孤儿工具数 = 0**，且新增一条断言测试**防止以后再出现孤儿**
- **现状证据**（实测快照 `3ce581c`）：16 个工具中 **6 个无人声明** ——
  `grade_answer`、`kb_stats`、`list_animations`、`log_mastery`、`log_qa`、`math_eval`
- **写入范围**：`backend/app/agents/knowledge.py`、`backend/app/agents/visualizer.py`、
  `backend/app/agents/solver.py`（各加工具声明/调用）、`backend/tests/test_kernel.py`、
  `scripts/check_contracts.py`（**新增第 7 节：孤儿工具检查**）
- **禁止触碰**：`backend/app/kernel/specs.py`（K1/K2 独占）
- **验收（可机验）**
  1. `python scripts/check_contracts.py` 第 7 节输出 **孤儿工具 0 个**
  2. 该检查**从"可见性提示"升级为门禁**：现在（K5 前）`check_contracts.py` 是
     `○ 孤儿工具 6/16` 的**提示**，K5 之后必须变成 `✔ 无孤儿工具` 的**断言**
  3. 故意删掉一个 `AgentSpec.tools` 条目 → 该检查**必须红灯**（负向测试）
- **依赖**：无（但 K3 完成后 `grade_answer` 会自然消失，**分工时注意重叠**） · **规模 S**

---

### 工作流 X · 悬浮窗（**只读**）+ 前端展示

> ⚠️ 按 `docs/20`，悬浮窗**只读**：不点按钮、不驱动动画、不在宿主页面上叠加元素。
> 本工作流里 `X2` / `X3` 属于悬浮窗；`X1` 是**主站与悬浮窗共用**的展示要求。

#### X1 · 显著展示验证级别 / 批改依据 ⬜ **P0** ★ 契约已要求、实现为零
- **交付物**：用户在回答区**看得见** A/B/C/D 级别、逐项检查、无法验证的部分
- **现状证据**（实测 grep）
  - `docs/02:61,77` 要求"前端必须**显著**展示验证级别，不能藏在折叠面板"；
    "笼统显示'已校验'是**不允许**的"
  - **实测：`frontend/` 全目录 grep `verification` → 零命中**
  - `frontend/app/components/trace.js` 的 `switch`（99–258 行）覆盖 11 种事件，
    **没有 `verification.report`**
  - `frontend/widget/assistant.js` 的 `switch`（924–983 行）同样**没有**
  - 结论：后端已在发、前端**完全静默忽略** → **`docs/02` 的承诺当前是空头支票**
- **要展示的四件东西**
  1. `levelLabel`（人话，如"未做自动验证，请自行判断"）
  2. `checks` 逐项，**按 schema 顺序**，含 `method`（它决定这条检查有多可信）
  3. `unsupported`（如实列出，**不允许静默忽略**）
  4. `disagreements` 非空时**高亮**（是纠错信号，不是错误）
- **写入范围**：`frontend/app/components/trace.js`、`frontend/widget/assistant.js`（展示位）、
  `frontend/app/features/**/index.js`、`frontend/app/_test_mount.mjs`（追加断言）
- **禁止触碰**：`contracts/**`、`backend/**`
- **红线**：**不得出现"已校验"这类笼统表述**（`docs/12:153`）
- **⚠️ 形态修正导致本任务升级为 P0**：按 `docs/20`，**批改与刷题成为主站核心功能** ——
  系统会直接给学生打"对/错"。**判错的代价比只是"答得不全"高得多**，
  所以"这个判定经过了几层验证、哪层没过"必须**显式可见**，不能藏在折叠面板里
- **适用位置**（两处都要）
  1. 悬浮窗的问答回答区
  2. **主站的刷题页 / 错题本**（`X5` / `X6`）—— 批改结果旁边要带依据
- **验收（可机验）**
  1. `node frontend/app/_test_mount.mjs` 追加用例：喂一个 mock `verification.report` 事件 →
     断言 DOM 出现 `levelLabel` 与逐项 `checks`
  2. D 级题断言出现"未做自动验证"字样
  3. `disagreements` 非空用例断言出现高亮类名
- **依赖**：C1（✅，事件结构已冻结） · **规模 M**

#### X2 · 抽取加固（**读屏能力**）+ SSE 人话提示 ⬜ **P0**
- **交付物**：5 类真实课件页抽取不跑偏；后端未起/Key 失效/请求超时都有**人话提示**，不白屏
- **为什么升级为 P0**：按 `docs/20`，**"读屏"就是悬浮窗的全部职责** ——
  抽不到内容，产品就等于不存在。这不是"体验优化"，是核心能力
- **现状证据**：`docs/12` M1 的四项（抽取加固 / 划词闭环 / 页内检索精度 / SSE 断线超时提示）
  **均未验收**；`frontend/widget/extractor.js` 暂无回归集
- **⚠️ 形态修正的影响**
  - **"划词闭环打磨"一项作废** —— 旧口径是"选区 → 弹「解释这段」按钮"，
    弹按钮属于**往宿主页面写元素**，违反"只读"（见 `docs/20` §二）
  - **新口径**：`extractor` **只读**选区内容并记录；用户**主动提问**时把选区带上去。
    不在宿主页面上叠加任何我们自己的可见元素
- **写入范围**：`frontend/widget/extractor.js`、`frontend/widget/**`、
  `frontend/widget/_test_extract.mjs`（新增）、`frontend/app/features/assistant/index.js`
- **禁止触碰**：后端（本任务纯前端）· `anim-bridge.js` 的**只读部分**（保留）
- **验收（可机验）**
  1. `node frontend/widget/_test_extract.mjs`：5 类页面（纯文本 / 多 iframe / 表格多 / 公式多 / 侧栏干扰重）
     各有一份人工标注正文，抽取结果**逐类断言**
  2. 三种失败场景（后端未起 / Key 失效 / 超时）各有断言"出现人话提示且非空白页"
  3. **只读断言**：在 5 类页面上运行后，宿主页面的 `document.body` **未被新增/修改任何节点**
     （对比运行前后的 DOM 快照）—— 这是本次形态修正的**新增红线**
- **依赖**：无 · **规模 M**

---

### 工作流 H · 主站（**功能重心**，见 `docs/20`）

> 按 `docs/20`，所有需要停留、操作、积累的功能都在主站；悬浮窗只负责"读屏 + 提问 + 入口"。
> **这五个任务几乎没有现成代码覆盖，是本轮的主要工作量。**
> 现有 `frontend/app/features/` 的 9 个展示页**不被删除**（`D1` 已裁定不剪枝），
> 它们将作为主站功能页的**改造基础**。

#### X4 · 主站入口壳 ⬜ **P0 —— 本轮明确要做的壳**
- **交付物**：一个能被悬浮窗跳进来的主站首页，含导航与各功能入口（题库 / 刷题 / 错题本 / 知识图谱 / 动画库 / 分布）
- **本轮范围**：**只做壳** —— 导航结构 + 路由 + 各功能页的占位与入口。
  壳里的功能由 `X5`–`X8` 分别填充，本轮不要求全做完
- **现状底子**：`frontend/app/` 已有可用的外壳（`core/router.js`、`bus.js`、`store.js`、
  9 个 `features/*/feature.json`）；`features/workbench/` 已是一个导航页，可作起点
- **写入范围**：`frontend/app/features/workbench/`（或新建 `home/`）、
  `frontend/app/features/*/feature.json`（加导航项）、`frontend/app/core/router.js`
- **禁止触碰**：`frontend/app/features.registry.js`（自动生成，跑 `gen_registry.py`）·
  `frontend/widget/**`（`X3` 独占）
- **验收（可机验）**
  1. `python scripts/gen_registry.py --check` 通过
  2. `node frontend/app/_test_mount.mjs` 通过（含新增的首页挂载断言）
  3. 手工：从悬浮窗点"进入主站"能落到首页，且首页上能点到 5 个功能入口
- **依赖**：无 · **规模 M**

#### X3 · 悬浮窗只读改造 + 主站入口 ⬜ **P0**
- **交付物**：悬浮窗变成"**只读探针 + 提问框 + 进入主站按钮**"，**不再控制宿主页面**
- **要删的（违反 `docs/20` §1.1）**
  | 位置 | 删什么 |
  |---|---|
  | `frontend/widget/anim-bridge.js` | `control()` 与 `btn.click()` 这条**驱动**路径（约 256 行的 `btn.click()`） |
  | `frontend/widget/assistant.js:340-362` | 动画**控制条**（play/pause/step/reset 按钮） |
  | `frontend/widget/assistant.js:869-872` | 代用户"下一步"的逻辑 |
  | 选区交互 | 划词**弹出按钮**的写法 → 改为只记录选区，用户主动提问时带上 |
  | 任何往宿主 `document.body` 追加的元素 | 遮罩层、探针挂载点等 |
- **要留的**：`anim-bridge.js` 的**只读**部分 —— `isAnimationPage()` / `state()` / `stateText()`
  （识别是哪个动画、演到第几步）。这是"我学到哪了"的数据来源，**保留**
- **要加的**：一个**进入主站**的入口按钮（跳 `X4` 的首页；地址不走写死，沿用五层地址解析）
- **写入范围**：`frontend/widget/anim-bridge.js`、`frontend/widget/assistant.js`、
  `frontend/widget/*_test_*.mjs`
- **禁止触碰**：`frontend/app/**`（`X4` 独占）
- **验收（可机验）** —— **只读是本任务的红线**
  1. `node frontend/widget/_test_render.mjs` / `_test_deploy.mjs` 全绿
  2. **新增只读断言**：注入到宿主页面后，宿主 `document.body` 的**节点快照与注入前一致**
     （允许我们自己的 Shadow DOM 宿主节点，但不得修改宿主已有节点）
  3. **新增无驱动断言**：注入到 8 个动画页后，**动画不因助手而前进**（对比 `#stepBtn` 被点击次数为 0）
  4. 只读能力仍在：能正确报出"当前是哪个动画、演到第几步、视频进度多少"
- **依赖**：**X4**（要有个地方可跳）· **规模 M** · ⚠️ 删驱动前先确认没有别的模块依赖 `control()`

#### X5 · 题库 + 刷题（**含二步提示**）⬜ P1
- **交付物**：主站上能做概率论题：出题 → 作答 → 判定 → **二步提示** → 看逐步解析
- **现状底子（很好）**：`backend/data/question_bank/core.json` 的题目结构**已经支撑刷题**：
  ```
  id · title · chapter · level · problem · final · steps · pitfalls · distractors · source
  ```
  `distractors` 里带**干扰项与针对性解释**，正好用于"猜错时告诉他为什么错"；
  `grade_answer` 引擎已能判定并给诊断（实测能认出"把 P(+|D) 与 P(D|+) 搞反"）
- **要做的**
  1. 后端：题库读取 / 按知识点与难度组卷（扩 `api/problems.py`，它已存在）
  2. 前端：`frontend/app/features/` 新建刷题页（可改造现有 `features/solver/`）
  3. **二步提示**：第一次提示给方向不给答案 → 第二次给关键一步 → 仍不会才给完整解析
     （提示内容取自 `steps[].display` 与 `pitfalls`）
- **写入范围**：`backend/app/api/problems.py`、`backend/data/question_bank/*.json`、
  `frontend/app/features/<刷题页>/`
- **禁止触碰**：`backend/app/tools/grader.py` 的既有算法（已核验，改动需 R3 复核）·
  `contracts/**`
- **验收（可机验）**
  1. 新增后端测试：组卷接口按 chapter / level 过滤正确；返回题目**不含答案字段**（防前端偷看）
  2. 新增前端断言：二步提示**逐级展开**，第一步不含最终答案
  3. 作答错误时展示**对应的干扰项解释**（用 `distractors`）
- **依赖**：**K3**（批改要能走到链路里）· **规模 L**

#### X6 · 错题本 ⬜ P1
- **交付物**：答错的题自动进错题本，按知识点归类，可重做、可移除
- **现状底子**：`learning.sqlite` 的 `qa_log` 表**已有 115 行真实数据**；
  `mastery` 表**结构已建但 0 行**（因为 `log_mastery` 无人调用 → 任务 `K4`）
- **要做的**
  1. 错题记录表（题 id / 知识点 / 错因 / 时间 / 重做结果）
  2. `K4` 把 `log_mastery` / `log_qa` 真正接进链路（**依赖**）
  3. 前端错题本页：列表 + 按知识点分组 + 重做入口
- **写入范围**：`backend/app/tools/learning_log.py`（扩表）、`backend/app/api/system.py`（统计接口）、
  `frontend/app/features/analytics/`（改造为错题本/学习数据页）
- **禁止触碰**：`backend/app/agents/_router.py`（`K3` 可能要用）
- **验收（可机验）**
  1. 答错一题后，错题表**新增一行**且知识点正确
  2. 重做正确后该错题状态**转为已订正**
  3. `/api/stats/{session_id}` 返回按知识点聚合的错题数
- **依赖**：**K4** · **规模 M**

#### X7 · 知识图谱 ⬜ P1
- **交付物**：概率论知识点网络图 —— 节点（知识点）+ 边（依赖关系）+ 按掌握度着色
- **⚠️ 与旧文档冲突**：`docs/14` §七 曾把"知识点 DAG 级联定位"列为**明确不做**；
  本次形态修正**推翻了那个决定**（`docs/20` §三 已记录）。任务书此处以本卡为准
- **现状证据**：**全仓库没有知识点依赖图**。`docs/09` §四 C2 要求"60–100 节点"，
  但该产物从未存在 → **本任务从零建**
- **要做的**
  1. 数据：知识点节点 + 依赖边（来源：教材目录 + `probstat.md` 的 9 章 18 节 +
     双人独立标注；`D3` 教材到位后可细化）
  2. 后端：图谱读取接口；把 `mastery` 的掌握度按知识点挂到节点上
  3. 前端：`frontend/app/features/` 新建图谱页（现有 `features/architecture/` 可改造）
- **写入范围**：`backend/data/knowledge_graph/*.json`（新增）、`backend/app/api/knowledge.py`、
  `frontend/app/features/<图谱页>/`
- **禁止触碰**：`contracts/**`；图谱数据**不要**塞进 `CORPUS_DIR`（会污染检索验收数字）
- **验收（可机验）**
  1. 新增测试：图谱 JSON 无悬空边、无环（或显式允许的环）、节点数 ≥ 60
  2. 每个节点都能关联到 `probstat.md` 的某个章节
  3. 前端：给定一份 `mastery` 数据，节点着色符合预期（有测试断言）
- **依赖**：`D3`（教材）**可先用自建知识点列表起步**，不阻塞 · **规模 L**

#### X8 · 动画库 / 分布可视化归位 ⬜ P2
- **交付物**：把现有 8 个动画与 8 种分布可视化做成**主站功能页**（而非"展示页"）
- **与 `X3` 的分工**：**动画驱动能力移到主站** —— 悬浮窗不再驱动动画（只读），
  但主站可以自由驱动（那是我们自己的页面，不受"不修改宿主"约束）。
  `anim-bridge.js` 的 `control()` 可**移到主站**复用，不必删除
- **现状底子**：`frontend/app/features/animations/`、`features/visualize/` 已存在且能用；
  `/api/animations`、`/api/distributions/*` 已就绪
- **写入范围**：`frontend/app/features/animations/`、`frontend/app/features/visualize/`、
  `frontend/assets/animations/manifest.json`
- **禁止触碰**：`frontend/assets/animations/_raw/**`（**只读，一字不改**）
- **验收（可机验）**
  1. `node frontend/app/_test_mount.mjs` 通过
  2. 主站动画页能驱动动画（play/step），且**原动画文件未被修改**（对比 SHA256）
- **依赖**：`X4` · **规模 S**

---

### 工作流 E · 评估

#### E1 · 题库扩到 ≥100 题（原 M4 基准）⬜ P1
- **交付物**：`backend/data/question_bank/` 扩到 **≥100 道**概率论题
  （题干 + 标准答案 + **知识点标注** + 难度 + 干扰项），**逐题 SymPy 独立复算**
- **⚠️ 形态修正让本任务更关键**：按 `docs/20`，**题库是主站的核心功能**（`X5` 刷题直接吃它），
  同时它兼任原来的"评测基准"角色 —— 一份数据两用，不做两遍
- **现状证据**：`core.json` 现在 **10 道**（`examples.json` 也是 10 条）；
  检索标尺 `scripts/retrieval_questions.tsv` **40 题**
- **为什么优先级实际很高**：它**不依赖教材**，且是 `X5` 刷题与 `C2` 验证的共同判据来源
- **写入范围**：`backend/data/question_bank/*.json`（扩充）、`scripts/verify_question_bank.py`、
  `scripts/eval_retrieval.py`、`scripts/retrieval_questions.tsv`
- **禁止触碰**：`contracts/**`；**题目结构不要改**（`X5` 与 `grader.py` 都依赖现有字段）
- **验收（可机验）**
  1. `python scripts/verify_question_bank.py` → **≥100 道全过**，含**典型错答有效性**校验
     （即"故意写错的答案必须被判错"，防止标准答案写宽了）
  2. 每题都有 `chapter` 与 `level`（`X5` 组卷依赖）
  3. 产出正确率报告并写入文档，作为回归基准
  4. 检索标尺 ≥120 条
- **依赖**：无，**可与全部任务并行** · **规模 L** · 💡 建议与 `X5` **同一人**做（数据与消费端一起）

---

### 工作流 D · 需决策或外部资源（不占技术排期）

#### D1 · M0 剪枝口径裁定 ✅ **已完成 —— 裁定：不执行剪枝（2026-09-13）**

- **原矛盾**：`docs/12:87` 说 M0（按 `docs/11` 删展示页）"前置，必须最先做"；
  但实测仓库里 9 个展示 feature、`/api/problems`、`/api/knowledge`、`/course/` **全都还在**，
  且 `README.md` 还**推荐新人先试 `/course/`**；而 `docs/12` 已把 M2 标 ✅
- **裁定结果：方案 (a) 已放弃剪枝**
  1. **代码现实** —— `docs/11` 建议删除的对象**全部仍在仓库里**，从未执行
  2. **重心已转移** —— M2 已完成，当前重心是**正确性保障链**（本任务书 C 系列），
     删展示页的收益远低于把 A/B 级验证做出来
  3. **删除有真实代价** —— 展示页是答辩与演示材料（轨迹回放 / 分布演示 / 课件页），
     且与 `docs/04` §2 的角色 Own 目录直接冲突
- **落地改动**
  - `docs/11` 加归档抬头 + 裁定结论 + **3 处实测错误更正**（错路径、误引会议结论、错 tag 名）
  - `docs/12` 的 M0 段改为"❌ 已裁定不执行"，并保留其中**仍然有效**的完成标准
  - 本任务书 §八 的 M 阶段对照同步
- **验收**：`docs/11`、`docs/12`、`docs/18` 三处对 M0 的说法一致 ✅
- **后续**：若将来因"启动变慢 / 维护成本上升 / 需要对外发精简包"要重新考虑剪枝，
  **另开新任务**，不要直接照着 `docs/11` 执行

#### D2 · 真实平台**读屏**验证（M5）🚫 **P0（等资源）**
- **交付物**：在**真实智慧树账号**的**真实课件**上验证"**能读到屏幕内容**"
- **⚠️ 形态修正抬高了本任务的重要性**：按 `docs/20`，读屏是悬浮窗的**全部职责**。
  如果真实平台上读不到正文（CSP / 跨域 iframe / 非常规 DOM），
  **悬浮窗就完全没有价值** —— 这不再是"体验好不好"，而是"产品成不成立"
- **为什么是最大未知数**：演示页 `/course/` 是我们自己仿造的友好页面；
  真实平台的 DOM 结构、CSP 策略、iframe 嵌套深度都可能让抽取直接失效
- **前置**：需要一个智慧树账号 + 老师配合（**本轮最该去要的资源**）
- **建议**：**先做一次最小验证**（只验证"脚本能被注入 + 能读出正文 + 能读选区"），
  不要等主站全做完才发现路走不通
- **写入范围**：验证结论落 `docs/notes/平台兼容性实测.md`（**该文件目前不存在**，
  `docs/05` 曾承诺产出但从未创建 —— 本任务负责补上）
- **验收**：该文档记录了 ≥1 个真实平台的实测结论，含"读到了什么 / 读不到什么 / 原因"
- **规模 M** · **阻塞条件**：账号 / 授权

#### D3 · 教材语料入库（L1）🚫
- **交付物**：教材原文进入 `CORPUS_DIR`，检索能指到"第几章第几节"
- **现状**：检索侧**已就绪**（两来源 L0 示例 + L1 教材、出处到章/小节/行号、
  mtime 签名自动重建索引、`/api/health` 报各来源份数），**只差教材本身**
- **格式**（照抄 `docs/13` §1.2）：`# chXX 章名` → 章；`## 小节名` → 检索片段与出处；
  公式保留 LaTeX 原文（BM25 靠符号命中）
- **⚠️ 陷阱**：格式示例、自撰笔记**不要**丢进 `CORPUS_DIR` —— 会被当教材正文索引进去，**污染验收数字**
- **阻塞条件**：老师提供教材电子版

---

## 四、避冲突规则

### 4.1 写入范围互斥（核心纪律）

**每个任务列出它 own 的文件；两个任务不共享可写文件。** 公共文件由**指定任务独占**：

| 共享文件 | 独占任务 | 其他任务要改怎么办 |
|---|---|---|
| `backend/app/kernel/specs.py` | **K1 → K2**（串行） | 提 PR 说明，等 K2 合完再动 |
| `backend/app/kernel/graph.py` | **C4**（fan-out） | C3 **禁止触碰**；确需改先证明链路不通 |
| `backend/app/kernel/events.py` | 当前无人独占（G1 已完成） | **新增事件必须同步 3 处**：`events.py` + `events.schema.json` + `contracts/CHANGELOG.md` |
| `contracts/**` | 语义变更只有 **R1** 能动 | 新增枚举值 / 可选字段任何人可提 PR；**必须写 CHANGELOG** |
| `backend/app/agents/_router.py` | **K3**（加批改意图） | 改动形态是"往 dict 加一个 key"，冲突面小但**必须提 PR 说明** |
| `frontend/app/components/trace.js` | **X1** | K2 只改后端，**不动前端** |
| `README.md`、`docs/11-16` | **G2** | 其他任务**不得**顺手改数字 —— 统一交 G2 |
| `frontend/app/features.registry.js` | **自动生成，禁止手工编辑** | 跑 `python scripts/gen_registry.py` |
| `frontend/assets/animations/_raw/**` | **只读** | 需要注入时由后端在响应时插（`api/raw_live.py`） |

### 4.2 分支与合并（`docs/04` §4，此处只留结论）

```
feature/xxx ──squash──▶ dev ──merge（绝不 squash！）──▶ main
```
`squash` 只能用在"合并后分支就删掉"的场合；`dev` 是长期分支，**squash 会让历史分叉**（已踩过）。

### 4.3 Definition of Done

- [ ] 代码在任务声明的**写入范围**内；碰了共享文件已在 PR 描述里单独说明
- [ ] **新增能力带了能拦住回归的测试**（测试是门槛，不是装饰）
- [ ] 本卡片"验收"里的**每一条命令都真跑过**，输出贴进 PR
- [ ] 契约若有变更：`events.py` + schema + `contracts/CHANGELOG.md` **三处同步**
- [ ] 失败**显式**：读不到内容 / 检索为空 / 校验不过 —— 都要**说出来**，不允许沉默降级
- [ ] 至少 1 人 Review；涉及 `kernel/` 或 `contracts/` 的必须有对应 Owner 参与

---

## 五、和上一版任务书的差异（为什么重写）

| 问题 | 上一版 | 本版 |
|---|---|---|
| **任务与理由混写** | A1 卡片 45 行，其中 25 行是落地记录与踩坑 | 任务压成 8 字段固定结构；**记录移入 §八 索引** |
| **状态过期** | A1/E1 标为待办，B1 标为未开工 | 全部**实测复核**，并给出「接续说明」表 |
| **基线数字分散** | §六 表 + 各卡片里再抄一遍（→ 必然漂移） | **只有 §七 一处**；卡片引用它，不再复写 |
| **验收不可机验** | 如"有量化记录""抽取结果可核对" | **每条给一条能跑的命令** + 期望输出 |
| **无 ID 体系** | A1/A2/B1/C1 与 docs/09 的 M1.1–M1.7 两套编号并存 | 统一 **工作流前缀 + 序号**；与 M 阶段的对照见 §八 |
| **缺"谁不能碰什么"** | 只有一张共享文件表 | 升为 **§四 避冲突规则**，与每张卡片的"禁止触碰"互锁 |
| **已完成任务占正文** | ✅ 任务仍占大段正面篇幅 | ✅ 收进 §二 总览 + §八 索引，正文只留未完成 |

---

## 六、基线数字（**唯一权威处**）

> 快照：分支 `dev` @ `3ce581c`。**所有命令先设** `$env:PYTHONIOENCODING="utf-8"`（中文 Windows 必需；
> `scripts/_console.py` 已自动切 UTF-8，但设了更保险）。G2 只需维护这一处的措辞，**改数必须重测**。

| 项 | 值 | 怎么测 |
|---|---|---|
| AgentSpec | **7** | `python scripts/check_contracts.py` §4 |
| ToolSpec | **16** | 同上 |
| **孤儿工具（无 Agent 声明）** | **6** —— `grade_answer`、`kb_stats`、`list_animations`、`log_mastery`、`log_qa`、`math_eval` | 比对 `AgentSpec.tools` 与工具全集 |
| 事件类型 | **14** | `contracts/events.schema.json` → `properties.type.enum` |
| artifact kind | **6**（`animation`/`chart`/`formula`/`steps`/`text`/`table`） | `events.schema.json` |
| 契约校验项 | **46** | `python scripts/check_contracts.py` |
| 端到端自检 | **25** | `python scripts/smoke_test.py` |
| 单元测试 | **100 收集 / 99 passed / 1 skipped** | `python -m pytest backend/tests -q` |
| CI 实际覆盖 | **100（全量）** —— G1 前只覆盖 52 | `.github/workflows/ci.yml` |
| 检索标尺 / 题库 | **40 题 / 10 道** | `scripts/retrieval_questions.tsv`、`question_bank/core.json` |
| `qa_log` / `mastery` 表 | **115 行 / 0 行** | `backend/data/learning.sqlite` |
| 知识点依赖图（DAG） | **不存在** | 全仓库无此产物 |
| 主站功能页 | **0 个**（`frontend/app/` 的 9 个是展示页） | `features.registry.js` |
| 前端展示验证级别 | **零命中**（`verification` 在 `frontend/` 下 grep 无结果） | `grep -r verification frontend/` |
| 页面挂载测试 | `node frontend/app/_test_mount.mjs` | — |
| 渲染器测试 | `node frontend/widget/_test_render.mjs` | — |
| 部署无关化测试 | `node frontend/widget/_test_deploy.mjs` | — |
| 注册表一致性 | `python scripts/gen_registry.py --check` | — |
| **事件流实测**（mock，solve 类提问） | 尾部为 `… tool.result → verification.report → artifact → agent.end → hitl.request` | 见 §七 |
| 已知最高风险 | **尚未在真实智慧树账号上验证** | 需外部资源（D2） |

### 已知限制（**不夸大**）

1. **A/B 级恒不可达** —— 不是 bug，是 C2 未做；当前所有题目实际落在 C 或 D
2. **工具门控形同虚设** —— `orchestrator` 能调到它没声明的 `kb_search`（K1 要修）
3. **验证级别用户看不见** —— 后端已发、前端静默（X1 要修）
4. **6 个工具是死代码**（K3/K4/K5 要修）
5. **CPU 上单次重排 ≈ 2.2 s** → 交互路径默认「融合不重排」（`docs/16`）
6. **悬浮窗当前会控制宿主页面** —— `anim-bridge.js` 有 `control()` + `btn.click()`
   （243 行）会点动画的播放/下一步按钮。**这与 `docs/20` 的"只读"定义冲突**，
   由 `X3` 改造。改造前**不要**把悬浮窗当"只读"来宣传
7. **主站功能基本为空** —— 题库只有 10 道（`core.json`）、无刷题页、无错题本、
   **无知识点依赖图（DAG 从未存在）**。`frontend/app/` 的 9 个页面是**展示页**，
   不是功能页 —— `X4`–`X8` 要做的正是把它们变成功能页
8. **`mastery` 表结构已建但 0 行** —— 因为 `log_mastery` 无人调用（K4）。
   `qa_log` 表已有 **115 行**真实数据可用

---

## 七、怎么自己复测（**不信本文档时照这里做**）

```powershell
cd 大创\ProbCrew
$env:PYTHONIOENCODING="utf-8"

# 1) 契约 + 注册表 + 基线数字
python scripts/check_contracts.py            # 期望：全部通过（46 项；其中孤儿工具是 ○ 提示）
python -m pytest backend/tests -q            # 期望：100 collected
python scripts/gen_registry.py --check

# 2) 孤儿工具（自己算，别信 §六 的表）
python -c "import sys;sys.path.insert(0,'backend');
from app.kernel.registry import discover_agents,discover_tools;
a=discover_agents().all(); t=sorted(x.id for x in discover_tools().all());
d=set(); [d.update(e.spec.tools) for e in a]; print([x for x in t if x not in d])"

# 3) 真实事件流（验证 C1/C3：verification.report 与 hitl.request 到底发没发）
#    见下面这段最小脚本 —— 起服务也可以，但脚本更快
```

```python
import os, sys, json
sys.path.insert(0, "backend"); os.environ["LLM_PROVIDER"] = "mock"
from fastapi.testclient import TestClient
from app.main import build_app
c = TestClient(build_app())
for q in ["求 P(A|B)，P(A)=0.3, P(B)=0.5，A、B 独立",   # solve  → 期望 hitl.request
          "解释一下贝叶斯公式",                        # knowledge → 期望无 hitl.request
          "证明 P(A∪B)=P(A)+P(B)-P(AB)"]:              # 证明题 → 口径待 D1/C3 裁定
    with c.stream("POST", "/api/chat/stream", json={"query": q, "session_id": "t"}) as r:
        types = [json.loads(l[6:]).get("type") for l in r.iter_lines() if l.startswith("data: ")]
    print(q[:12], "| verification.report:", "verification.report" in types,
          "| hitl.request:", "hitl.request" in types)
```

```powershell
# 4) 前端是否真的没处理验证级别（X1 的依据）
grep -rn "verification" frontend/          # 期望：零命中（Linux/macOS/Git Bash）
```

---

## 八、与 M 阶段的对照 + 历史记录索引

### 8.1 任务 ID ↔ `docs/12` 阶段

| 任务 | 对应 M 阶段 · 子项 |
|---|---|
| C1 | M3（强制校验报告）· M1.7（存疑标注） |
| C2 | **M1.1 形式化** · **M1.2 数值校验** · **M1.3 符号校验** |
| C3 | M3.4（HITL 闸门） |
| C4 | M1.5（多路求解） |
| E1 | **M4**（题库 ≥100 道，兼任评测基准） |
| X1 | M3 红线（不得出现"已校验"） |
| X2 | **M1.1–M1.3**（抽取加固 / 页内检索 / SSE 提示） |
| K3 | 立项书 M3（批改闭环） |
| D1 | **M0**（骨架剪枝口径） |
| D2 | M5（真实平台验证） |
| D3 | M2.1（语料结构化） |

> ⚠️ **`X3`–`X8`（主站五件 + 悬浮窗只读改造）没有对应的 M 阶段。**
> 原因是 `docs/12` 的阶段划分写于**形态修正之前**，它里面没有"主站"这个概念。
> 这五件是本次修正新提出的，`docs/20` §三 已记录。
> **`docs/12` 需要补一个阶段来容纳主站**（归 `G2` 处理），否则阶段计划与任务书对不上。

> 📌 上一版的 A2 → 本版 **K1**；A3 → **K2**；B1 → **C3**（改写）；B2 → **X1**；
> C1/C2/C3（上一版）→ **K3 / K4 / K5**；D1/D2 → **E1 / X2**；E1 → **G1** ✅；E2 → **G2**；E3 → **D1**。
> **重编号的原因**：上一版按"处理顺序"编号（A/B/C/D/E），与本任务书按"工作流"组织不符，
> 且已完成的 A1/E1 占用了字母，新任务无法自然续号。

### 8.2 上一版中的落地记录（考古用，正文不再展开）

| 已落地内容 | 提交 | 关键踩坑（不要重蹈） |
|---|---|---|
| C1 验证报告契约收敛 | `3ce581c` | ① 级别是**两道封顶**（题型 × 手段木桶），不是单一判据；② `needs_human`/`notes`/`draft` **不进报告**（`additionalProperties:false`），仍在 `verdict` 里供编排使用；③ `unsupported` **只列"能力所不及"**，不混入"某条检查没通过"；④ `verifier.py` 模块级有 `async def run(...)`，若在 `verify()` 里用局部变量 `run`，`run.run_id` 会解析成**函数对象** → 用 `state["run_id"]` |
| G1 CI 与基线修复 | `a0923d8` | ① 编码问题实际波及 **12 个脚本**（不止 3 个），统一用 `scripts/_console.py::utf8_output()`；② 构造器检查必须**由事件枚举动态派生**，硬编码会假绿；③ `requirements-dev.txt` 里 **pytest 一直是注释状态**，CI 改全量后才暴露 |
| — 部署无关化 | `2e96161` | 油猴 `@require` 相对路径 + 五层地址解析 |
| — 混合检索（BGE-M3） | `2e96161` | ① `tokenizer.json` 缺失会让 `AutoTokenizer` **静默**回退慢速实现并把中文全编码成 `<unk>`（向量恒等）；② BGE-M3 官方池化是 **CLS**，用 mean pooling 会掉区分度 |

---

## 九、本任务书覆盖不到的（需要人或外部资源）

1. **D2 真实平台验证**：需智慧树账号 / 老师配合 —— 当前**最大未知数**，且不在任何技术任务里
2. **D3 教材语料（L1）**：`CORPUS_DIR` 接口已就绪，**等老师提供教材**
3. **D1 M0 口径**：需团队一句话决定
4. **B 线（学生数据 / 教师端）**：**挂起中**，不进入本轮分工（触发条件见 `docs/09` §六）
5. **C2 的形式化准确率**：需要一份 50 题的标注样本 —— 与 E1 有重叠，**建议由同一人先做 E1 的题库**，
   顺带产出形式化标注，避免做两遍
