# 原型声明 · PROTOTYPE NOTICE

> **请在使用、演示或分发本软件之前完整阅读本声明。**
>
> 本软件是**大学生创新创业训练计划项目的阶段性原型（prototype）**，
> 不是可用于生产环境的成品，也**不适合接入真实教学数据**。

---

## 一、一句话说明

这是一个**技术可行性验证原型**：用来证明"多 Agent 协作 + 页面感知 + 人机协同"
在概率论学习场景下**可以跑通**，并作为后续迭代的骨架。
它的价值在于**架构与机制**，不在于内容完备度或工程成熟度。

---

## 二、这是什么 / 不是什么

| 它是 | 它不是 |
|------|--------|
| 一个可运行的多 Agent 协作原型（7 个 Agent / 15 个工具） | ❌ 一个可以交给学生日常使用的学习产品 |
| 一个"能读懂当前页面"的助手组件的技术验证 | ❌ 一个已经通过教学效果验证的教具 |
| 一份可扩展的工程骨架（契约优先 + 自动发现） | ❌ 一个功能完整、经过压力测试的系统 |
| 8 个既有交互动画的接入方案与统一控制层 | ❌ 一套原创的教学内容体系 |

---

## 三、已实现 / 未实现（诚实对照）

> 我们刻意不做"看起来很厉害"的模糊表述。以下清单可直接用于评审提问。

### ✅ 已实现并有自动化测试守护

| 能力 | 验证方式 |
|------|---------|
| LangGraph 编排：路由 → 条件边并行 fan-out → 校验 → HITL → 聚合 | `scripts/smoke_test.py` |
| 7 个 Agent：Orchestrator / Knowledge / Problem Solver / Verifier / Visualization / Analytics / Page Tutor | `/api/agents` 自动发现 |
| **LangGraph 原生 `interrupt()` 人机协同**，用户修正**回流进最终答案** | `smoke_test.py` §3 |
| 生成/验证分权：Verifier 独立复核步骤完整性、教材依据、公式定界符 | 单测 + 集成测试 |
| 页面上下文读取：正文 / 标题 / 公式 / 选中文字 / 视频进度 / 动画状态 | `tests/test_page_context.py`（15 项） |
| **跨 iframe 上下文汇总**（超星课件的关键难点） | 同上 |
| 8 个既有动画：同源 iframe 远程驱动，**原文件一字未改** | `smoke_test.py` §5 |
| 8 种分布的 SymPy 符号推导，期望/方差与解析解一致到 1e-9 | `tests/test_kernel.py` |
| Mock 模式：无 API Key / 断网可完整演示 | `providers/mock.py` |
| 免构建前端 + Shadow DOM 隔离的悬浮窗 | 人工验证 |

**测试规模**：后端 56 项 + 前端 32 文件语法检查 + 渲染器 6 项，全部通过。

### ❌ 未实现（迭代二或更晚）

| 未实现 | 影响 | 说明 |
|--------|------|------|
| **未在真实课程平台（超星/智慧树）上验证过** | 产品形态的**前提**未经证实 | 演示页 `/course/` 是我们自己仿造的友好页面。真实平台的 DOM 结构、CSP 策略、iframe 嵌套深度都可能让助手退化成普通聊天框。**这是最高风险项** |
| 知识库只有 5 章 18 节（demo 级） | 覆盖度不足 | ch01–ch05；完整课程应有 9 章 |
| 检索是本地 Markdown + 中文 bigram，**不是向量检索** | 召回质量有限 | 接缝已冻结在 `kb_search`，换 Qdrant 时 Agent 不用改 |
| **无知识点依赖图（DAG）** | 做不到"级联定位薄弱根源" | 只能做单点正确率排序 |
| **无做题/批改闭环** | 学习数据页的"掌握度"没有数据来源 | 当前 `mastery` 表是空的 |
| **无用户系统、无鉴权** | 任何人都能调用；会话 id 可伪造 | 上生产前必须补 JWT |
| **无限流** | 一个脚本就能刷光 API 额度 | — |
| CORS 为 `*` | 生产环境安全隐患 | 见 `backend/app/main.py` |
| **运行记录在内存中** | 服务重启即清空 | 换 `SqliteSaver` 即可，但未做 |
| 未做并发测试 | 单进程 uvicorn，多人同时用会排队 | — |
| 无 RAGAS 等自动评估 | 只有自建断言，没有质量指标 | 轨迹已留档，可作为数据集 |

---

## 四、⚠️ 数据流向与隐私（重要）

### 开启页面读取时，你的页面内容会离开你的电脑

```
浏览器里的课程页面
   ↓ ① 助手抽取正文、标题、公式、你划选的文字、视频进度
   ↓ ② 随每次提问 POST 到后端（http://127.0.0.1:8000 或部署的服务器）
   ↓ ③ 后端把你的问题 + 页面内容一起发给大模型厂商
        （DeepSeek / 通义千问 / 智谱 GLM，取决于你的配置）
   ↓ ④ 厂商按自己的隐私政策处理这些内容
```

**请据此判断**：

- ❌ **不要在含有以下内容的页面上开启助手**：个人身份信息、未公开的内部资料、
  受保密协议约束的试题、他人隐私数据；
- ⚠️ 用第三方大模型厂商 API 时，内容会离开本地网络，受厂商条款约束；
- ✅ 面板标题栏的 **📖 按钮**可以随时关闭页面读取 ——
  关闭后不再抽取、不再上报，只用手头的教材知识库回答；
- ✅ 也可以用 Mock 模式（`LLM_PROVIDER=mock`）演示：此时**不发起任何外部请求**。

### 本地会留下什么

| 文件 | 内容 | 是否随包分发 |
|------|------|-------------|
| `backend/data/learning.sqlite` | 你的提问原文 + 完整答案 + 会话 id | ❌ **打包脚本会排除**（属于隐私数据） |
| 内存中的运行轨迹 | 每次运行的全部事件（含页面内容摘要） | ❌ 重启即清空 |
| 浏览器 localStorage | 会话 id、皮肤选择、悬浮球位置 | 仅存本机 |

打包脚本会把 `*.sqlite` 全部排除，并在分发前扫描密钥 —— 已用假密钥验证过它会中止打包。

---

## 五、⚠️ 合规提示

### 5.1 教材与题库内容

知识库（`backend/knowledge_base/`）的内容**必须来自**：
- 课程教师提供的材料，或
- 团队自行整理的内容，或
- 明确允许使用的开放资源

**不得**：
- 分发教材原始 PDF（版权）；
- 把商业习题册的题目原样录入并公开分发。

`docs/05-开发计划.md` 的 P2 阶段已把"教材获取与合规确认"列为 R1 的任务。

### 5.2 在第三方平台上使用（超星、智慧树等）

**这是本原型最需要谨慎对待的一点。**

通过油猴脚本向第三方平台注入代码，**可能违反该平台的用户协议**：

- 建议**只在自己的账号、自己选修的课程上做技术验证**；
- 正式推广前，应向平台或学校信息化部门报备；
- 长期路线应是"与平台合作接入"或"自建课程站"，而不是注入；
- 本仓库提供的油猴脚本默认 `@match *://*/*`（放开全部站点）是**为了方便调试**，
  正式使用必须收窄到指定域名（见 `docs/06` §2.2）。

### 5.3 学生数据

如果有人真的用了起来，问答记录属于**学习行为数据**，在国内属于个人信息，
受《个人信息保护法》约束。当前原型：
- 没有明示同意流程；
- 没有数据删除入口；
- 没有加密存储。

**因此再次强调：不要接入真实学生数据。**

### 5.4 内容准确性

- 分布性质、期望、方差等**数值与公式来自 SymPy 符号推导**，可复核；
- 但 **LLM 生成的讲解文字、解题步骤、易错点分析未经学科教师审核**，
  可能存在错误或不严谨之处；
- Verifier 只能做有限的自动校验（步骤数、教材依据、公式定界符），
  **不能替代专业判断**；
- **不得**将本系统的输出直接作为教学结论、评分依据或考试标准。

---

## 六、使用限制与免责

1. 本软件按"现状"提供，**不附带任何明示或默示的担保**，
   包括但不限于适销性、特定用途适用性、不侵权的担保。
2. 使用者自行承担使用风险。因使用本软件产生的任何直接或间接损失，
   项目组不承担责任。
3. 因调用第三方大模型 API 产生的**费用**由使用者自行承担。
4. 使用者应自行确保其使用方式符合所在学校/机构的规章制度与法律法规。
5. 本项目为教学研究用途，**不得用于商业目的**（除非另行取得授权）。

---

## 七、第三方组件与许可

| 组件 | 许可 | 用途 | 是否随包分发 |
|------|------|------|-------------|
| [KaTeX](https://katex.org/) | MIT | 前端公式渲染（本地 vendor，离线可用） | ✅ 是 |
| [FastAPI](https://fastapi.tiangolo.com/) / [Starlette](https://www.starlette.io/) | MIT / BSD-3 | Web 服务 | 由 pip 安装 |
| [Uvicorn](https://www.uvicorn.org/) | BSD-3 | ASGI 服务器 | 由 pip 安装 |
| [Pydantic](https://docs.pydantic.dev/) | MIT | 数据校验与配置 | 由 pip 安装 |
| [LangGraph](https://github.com/langchain-ai/langgraph) / [LangChain](https://www.langchain.com/) | MIT | 多 Agent 编排 | 由 pip 安装 |
| [SymPy](https://www.sympy.org/) | BSD-3 | 符号计算 | 由 pip 安装 |

**8 个交互动画**（`frontend/assets/animations/_raw/`）为本项目组所有，
随包分发**不修改、不署名改动**，保持自包含可独立运行。

---

## 八、版本与反馈

| 项 | 值 |
|----|-----|
| 版本 | v0.1.0（原型） |
| 阶段 | 对应 `docs/05-开发计划.md` 的 **P0 骨架就绪** 完成 |
| 包内容校验 | 见包内 `PACKAGE-INFO.txt`（含逐文件 SHA256） |
| 构建方式 | `python scripts/package.py --version 0.1.0` |

**反馈问题**时请附上：
- `http://127.0.0.1:8000/api/health` 的输出（能看出 provider、Agent 数、知识库规模）；
- 浏览器控制台的报错；
- 「运行轨迹」页里对应那一轮的 run_id（后端可用 `/api/runs/{run_id}/timeline` 复现完整过程）。

---

## 九、给评审老师的一句话

> 这个原型验证的是**架构与机制**：多 Agent 如何分权协作、系统如何读懂学生当前在看什么、
> 关键结论如何走到人机协同。内容和工程成熟度都还是 demo 级，
> 已知限制在第三节逐条列明，最高风险是"尚未在真实课程平台上验证" ——
> 这也是下一阶段（P1）的第一优先级任务。

---

*本声明随原型一同分发。如本声明与实际代码行为不一致，以代码为准，并请反馈以便修正。*
