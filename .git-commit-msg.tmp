docs: B线技术方案 —— 学习者建模 + 教师端

方向选择：在四条拓展线索中选 B线（教育测量 + 教师端）。
理由：这是唯一「时间站在项目这边」的方向（数据越多模型越准），
且能满足三条深度判据 —— 特别是它有**公开基准数据集**
（ASSISTments / EdNet）可以对标，模型正确性有文献区间可比，
不像 A线（可验证数学推理）的对错要自己定义。

方案要点：
- 架构三层 + 一条闭环（教师端 → 学习者模型 → 数据层 → 反哺生成）
- 四个必答设计问题：数据从哪来 / 冷启动 / 怎么证明有用 / 隐私合规
- P0–P7 分阶段计划，每阶段含交付物与可检查的验收标准
- 明确 P0（做题与批改闭环 + 数据契约）是地基，不做完不准开工建模
- 风险登记 8 条，最高风险是拿不到真实数据与知情同意

关键技术判断：
- attempts 表必须含 hint_used / duration_ms。否则 BKT 会把
  「独立答对」与「被提示后答对」当成同一个观测，模型直接失效
- 用合成数据（已知参数反推）验证 BKT 实现，不阻塞于真实数据
- 学生作答数据是否发给云端 LLM 是必答题，给了四条路线并推荐混合方案
- 教师端设计必须先访谈老师，不要自己猜，否则容易白做

现有代码大部分可复用，属增量而非重写：
kernel / agents / tools / domain / features 自动发现机制全部沿用，
新增 domain/{bkt,irt,knowledge_graph,diagnosis,scheduler}.py
与 agents/{grader,tutor}.py。
